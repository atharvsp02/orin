# Self-hosted Cognee OSS — NOT Cogwit. Verified working config (cognee 1.2.2).

ENABLE_BACKEND_ACCESS_CONTROL=true
COGNEE_SKIP_CONNECTION_TEST=true

# --- Feedback learning (improve()) ---
DEFAULT_FEEDBACK_INFLUENCE=0.15   # must be >0 for maintainer feedback to affect retrieval (default 0.0 ignores it)
AUTO_FEEDBACK=false               # default true adds one extra LLM call per answered turn

# --- LLM ---
LLM_PROVIDER=openai
LLM_MODEL=openai/gpt-4o-mini
LLM_API_KEY=

# DeepSeek production configuration:
# LLM_PROVIDER=custom
# LLM_MODEL=deepseek/deepseek-chat
# LLM_ENDPOINT=https://api.deepseek.com/v1
# LLM_API_KEY=

# OpenRouter alternative (many models, no per-model daily cap; LiteLLM routes via the prefix):
# LLM_PROVIDER=custom
# LLM_MODEL=openrouter/google/gemini-2.5-flash
# LLM_API_KEY=sk-or-...
# OPENROUTER_API_KEY=sk-or-...

# --- Embeddings ---
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=openai/text-embedding-3-small
EMBEDDING_DIMENSIONS=1536
EMBEDDING_API_KEY=

# --- Persistent storage (mounted volume) ---
DATA_ROOT_DIRECTORY=/data
SYSTEM_ROOT_DIRECTORY=/data/system
