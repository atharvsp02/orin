# orin-web

Next.js dashboard (deploys to Vercel) — **Milestone B**.

Scaffold in place when ready:

    npm create next-app@latest . -- --ts --app

Then build: "Login with GitHub" (OAuth) → `GET /user/installations` → connect repos,
upload docs, edit rules, choose an LLM provider, and view graph stats / Cognee `visualize()`.
